spring:
  application:
    name: greeting-service

greeting-service:
  greeting: "Olá"
  default-value: "Mundo Development"

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'